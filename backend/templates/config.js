// API Configuration
// Using relative URLs since frontend and backend are now served from same domain
const API_BASE_URL = '';  // Empty string for same-origin requests

// No need for different URLs - everything is served from the same Railway service

// Export for use in other files
window.API_BASE_URL = API_BASE_URL;
